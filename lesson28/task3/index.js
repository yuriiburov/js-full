export const mult = (argFirst) => (argSecond) => argFirst * argSecond;

export const twice = (arg) => arg * 2;

export const triple = (arg) => arg * 3;

// console.log(mult(3)(3));
// console.log(twice(3));
// console.log(triple(3));
